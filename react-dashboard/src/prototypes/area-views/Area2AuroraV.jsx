/** prototypes/area-views/Area2AuroraV.jsx — Vista temporal: Aurora Vertical aplicado a Área 2. Ver AreaVariantView.jsx. */
import { AreaVariantView } from "./AreaVariantView.jsx";

export default function Area2AuroraV({ onNavigate }) {
  return <AreaVariantView areaId="area2" variant="aurora-v" onNavigate={onNavigate} />;
}
