/** prototypes/area-views/Area2Aurora.jsx — Vista temporal: Aurora Hero aplicado a Área 2. Ver AreaVariantView.jsx. */
import { AreaVariantView } from "./AreaVariantView.jsx";

export default function Area2Aurora({ onNavigate }) {
  return <AreaVariantView areaId="area2" variant="aurora" onNavigate={onNavigate} />;
}
