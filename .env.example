# Plantilla de configuracion del backend puente.
#
# Copiala a `.env.local` para desarrollo, o usala como referencia del entorno
# de produccion. Este archivo SI se versiona: no debe contener ningun secreto.
#
#   node --env-file=.env.local backend/server.mjs
#
# Resumen de variables y contratos en backend/README.md; validacion en backend/config.mjs.

# ── ICONICS ───────────────────────────────────────────────────────────
# Sin ICONICS_API_BASE la API responde 500. Si no es una URL absoluta
# valida, el servidor no arranca y dice cual es el problema.
ICONICS_API_BASE=https://tu-servidor/fwxapi/rest/v1
# Punto que leen /api/iconics/data y /api/context cuando no se indica otro.
ICONICS_POINT_NAME=

# ICONICS_USERNAME / ICONICS_PASSWORD: el login NORMAL no las usa. Desde el
# Plan 20 cada tecnico entra por la pantalla de login con SU PROPIA cuenta de
# ICONICS (POST /api/sesion): el puente ya no mantiene una identidad de
# maquina para leer la planta. Estas dos variables solo se leen en dos
# caminos que si necesitan una identidad sin persona detras:
#
#   - ICONICS_FAKE=true (mas abajo), para desarrollar sin servidor.
#   - Los scripts/verificar-*.mjs, que necesitan credenciales fijas para
#     poder correr sin intervencion humana.
#
# En un despliegue normal de produccion, deja las dos VACIAS.
#ICONICS_USERNAME=usuario-de-prueba
#ICONICS_PASSWORD=

# Cuanto dura una sesion de PERSONA por inactividad, y cuantas conviven a la
# vez. Ver backend/sesiones/registro.mjs y docs/PLAN-20-ASISTENTE.md §4.3.
# Reiniciar el backend cierra todas las sesiones: no hay base de datos que
# las recuerde, a proposito (CLAUDE.md §2).
#SESION_TTL_MINUTOS=60
#SESION_MAX=32

# Escritura sobre la planta. Por defecto DESHABILITADA: el puente escribe
# con el TOKEN DE LA PERSONA que hizo login, asi que quien no tenga permiso
# de escritura en ICONICS ya recibe un 403 del propio servidor de planta;
# esta variable es la segunda puerta, del lado del servidor puente. En
# produccion no se pone esta linea; en desarrollo se pone a "false" para
# poder controlar la bomba desde el asistente.
# Ojo: `controlar_bomba` respeta las DOS puertas. Con esto sin poner, se
# niega a encender o apagar la bomba y lo explica en la respuesta, aunque la
# cuenta de ICONICS de quien pregunta tenga permiso de sobra.
#ICONICS_READ_ONLY=false

# Transporte SIMULADO (Plan 14 §7.1): sirve las DOS maquinas sin salir a la
# red -las ocho senales de agua del tanque y los 73 puntos del sistema de
# vibraciones, con sus contadores de alarma-, con la misma firma que
# iconics/client.mjs. Con esto en "true" no hace falta ICONICS_API_BASE:
# el backend entero -historiador, asistente- funciona sobre datos
# inventados. Sirve para trabajar sin depender de que el servidor de planta
# este alcanzable. Con POST /api/sesion aceptando cualquier credencial no
# vacia (Plan 20 §4.4), tampoco hace falta ICONICS_USERNAME/PASSWORD para
# entrar.
#
# Reproduce tambien los fallos documentados del servidor real, no solo los
# datos buenos: las tres senales que devuelven la serie de la temperatura,
# el tope de 100 muestras por peticion, huecos y calidad mala ocasionales.
# Ver backend/iconics/fakeClient.mjs.
#
# NUNCA lo actives en produccion: ningun dato de este proceso vendria de la
# planta, y el arranque lo avisa por log a proposito para que no pase
# desapercibido.
#ICONICS_FAKE=true

# ── Servidor ──────────────────────────────────────────────────────────
PORT=3001
# debug | info | warn | error  (o silent, para las pruebas).
# Se admite en mayusculas: DEBUG, INFO, WARN, ERROR.
# En terminal el log sale coloreado; fuera de terminal, una linea JSON por
# evento, que es lo que un recolector sabe indexar. Las contrasenas y los
# tokens se sustituyen por [redactado] pase lo que pase.
LOG_LEVEL=INFO
# Build del frontend que se sirve. Relativo a la raiz del repo, o absoluto.
STATIC_DIR=react-dashboard/dist
# Version del despliegue. Configurala explicitamente en el entorno; se ve en
# /api/health y es lo primero que hace falta cuando alguien reporta un fallo.
APP_VERSION=dev
# Cambia a production en el servidor: endurece comprobaciones de arranque.
NODE_ENV=development

# ── Red ───────────────────────────────────────────────────────────────
# El backend escucha en 0.0.0.0, asi que el puerto es alcanzable desde la red
# en cuanto el firewall lo permita. En Windows, una sola vez y como
# administrador: scripts/exponer-en-red.ps1
#
# Origenes autorizados a llamar a la API desde OTRO origen, separados por
# comas. Se deja VACIO: no hace falta CORS en ninguno de los dos despliegues,
# porque en los dos la API vive en el mismo origen que la pagina. En planta
# porque el backend sirve el bundle; en desarrollo porque el dev server de
# Vite reenvia /api al backend (`server.proxy` en vite.config.js).
#
# Si algun dia hace falta, se listan uno a uno con esquema y puerto. Ojo: la
# comparacion es por igualdad exacta y NO existe el comodin `*`; ponerlo
# autoriza a un origen llamado literalmente "*", o sea a nadie.
#CORS_ORIGINS=http://localhost:5173

# Origenes autorizados a EMPOTRAR esta aplicacion en un <iframe> propio
# (frame-ancestors de la CSP, y X-Frame-Options mientras esta vacio). VACIO
# por defecto: nadie puede enmarcarnos, porque un tablero de planta dentro de
# un iframe ajeno deja los clics del operador a merced de quien puso el
# iframe (ver backend/http/plugins/seguridad.mjs).
#
# Existe para el caso contrario y deliberado: un HMI nativo de ICONICS
# (AnyGlass/GraphWorX) que muestra este asistente como una pantalla mas de su
# propio proyecto, dentro de SU iframe. Se lista el origen exacto del HMI,
# igual formato que CORS_ORIGINS -separados por comas, esquema y puerto-.
#
# A DIFERENCIA de CORS_ORIGINS, aqui el comodin `*` SI es peligroso de verdad
# -la lista se inyecta tal cual en la CSP, y "frame-ancestors *" es sintaxis
# real de "cualquiera puede enmarcarme"-, asi que el arranque falla si lo
# detecta, en vez de filtrarlo en silencio.
#FRAME_ANCESTORS=https://bms-server

# SSO silencioso: la redirect_uri de un login que NO pide usuario y
# contrasena porque el navegador ya trae la cookie de sesion de ICONICS
# puesta -entro por el HMI nativo (AnyGlass/GraphWorX), y este asistente vive
# empotrado ahi mismo en un iframe-. VACIO por defecto: sin esto, la funcion
# entera esta apagada y GET /api/sesion/silenciosa/iniciar responde
# {habilitado:false} -mismo trato que IA_BASE o RAG_UPLOAD_ENABLED.
#
# Tiene que ser una URL de ESTE backend, registrada A MANO en ICONICS
# (Workbench -> Security -> Global Settings -> Web Login -> "In-house
# application Relying Party Redirect URIs"): una que no coincida EXACTA no
# falla aqui, falla en ICONICS con un error que no dice que backend lo pidio.
#
# OJO CON EL ORIGEN DE ICONICS_API_BASE: el intento silencioso pide el
# authorize al MISMO origen que ICONICS_API_BASE. Si el HMI nativo sirve
# desde "https://bms-server" pero ICONICS_API_BASE apunta a
# "https://localhost" -aunque sea la misma maquina-, la cookie de sesion de
# AnyGlass NUNCA viaja: para el navegador son origenes distintos. Los dos
# tienen que coincidir con el dominio donde el tecnico realmente entro.
#SSO_REDIRECT_URI=http://localhost:3001/auth/silencioso

# Ponlo a true SOLO si hay un proxy inverso delante. Sin esto, el limitador
# ve la IP del proxy para todos los clientes y cuenta a la planta entera como
# uno solo. Con el puente expuesto directamente debe quedarse en false,
# porque X-Forwarded-For la falsifica cualquiera.
#TRUST_PROXY=true

# Limite por cliente. El defecto (300/min) corta un bucle desbocado sin
# molestar a un wallboard, que gasta ~4-12 peticiones/min.
# Las sondas de salud (/api/health*) quedan FUERA del limite a proposito: un
# orquestador las llama cada pocos segundos y gastarles cuota significaria que
# un reinicio del contenedor arranca con la sonda ya cortada.
#RATE_LIMIT_MAX=300
#RATE_LIMIT_WINDOW_MS=60000

# ── Ajuste fino ───────────────────────────────────────────────────────
# Corte de cualquier llamada saliente hacia ICONICS. Subelo si el servidor
# de esta planta es lento; nunca lo quites.
#UPSTREAM_TIMEOUT_MS=15000

# Caches de las lecturas en lote: colapsa en una sola llamada lo que piden
# todas las pantallas a la vez. 0 la desactiva.
#BATCH_CACHE_TTL_MS=2000

# ── Asistente de lenguaje natural (Planes 6 y 7) ──────────────────────
# Base de llama-server. VACIA por defecto, y ese defecto importa: sin ella
# /api/chat responde 503 y lo dice en la unica pantalla que hay. Una
# instalacion que nadie configuro no expone un asistente a medias.
#
# llama-server tiene que escuchar en 127.0.0.1 (no en 0.0.0.0): no lleva
# autenticacion de ninguna clase. Y hay que arrancarlo con --jinja, o el
# modelo no ve las herramientas y contesta de memoria.
#IA_BASE=http://127.0.0.1:8080

# Corte de la llamada al modelo. NO reutiliza UPSTREAM_TIMEOUT_MS a
# proposito: son dos escalas distintas, y 15 s es holgado para ICONICS pero
# ridiculo para un modelo de lenguaje.
#
# El defecto queda holgado aunque hoy sobre. Con el Qwen 4B, que cabe entero
# en la 4060, una respuesta son 4-5 s; pero basta cambiar el -m de
# llama-server por un modelo que no quepa para volver a descarga parcial en
# CPU y a los 30-90 s del 9B. Este numero no deberia obligar a tocar nada
# cuando eso pase.
#IA_TIMEOUT_MS=180000

# Tope de tokens de la respuesta. Cuidado: en Qwen3.5 los tokens de
# razonamiento salen por `reasoning_content` pero gastan de este mismo
# presupuesto, asi que un tope corto se lo come el modelo pensando y deja la
# burbuja vacia. La red de seguridad del backend redacta si eso pasa.
#IA_MAX_TOKENS=512

# Modelos entre los que se puede elegir desde el panel, separados por comas.
# El PRIMERO es el que se usa mientras nadie elija otro.
#
# VACIO POR DEFECTO, y ese defecto es la decision: sin esto el panel no pinta
# ningun selector, que es lo correcto en la instalacion normal de un solo
# modelo. Un desplegable de una sola opcion promete una eleccion que no existe.
#
# Solo tiene efecto si llama-server se arranco con --models-preset:
#
#   llama-server --models-preset model.ini --jinja --host 0.0.0.0 --port 8080
#
# Arrancado a la manera clasica (-m ruta.gguf) el campo `model` de cada
# peticion se IGNORA, asi que un selector no cambiaria el modelo que responde:
# diria una cosa mientras contesta otra.
#
# Los nombres tienen que ser los identificadores exactos que publica el
# servidor en GET /v1/models —los nombres de seccion del .ini de presets—.
# Compruebalos antes: uno que el servidor no conozca no da error, cae en su
# modelo por defecto en silencio.
#
#   curl http://TU-SERVIDOR:8080/v1/models
#
# OJO: los modelos se cargan bajo demanda y no caben dos a la vez, asi que
# cambiar de modelo hace que la PRIMERA respuesta tarde bastante mas mientras
# se descarga uno de la VRAM y se carga el otro. Por eso el modelo activo es
# uno solo para TODO el servidor y no uno por pantalla.
#IA_MODELOS=qwen-3.5-4B,qwen-3.5-9B

# Nombre del modelo cuando IA_MODELOS esta vacio. Informativo: con un solo
# modelo servido, llama-server ignora este campo y solo se ve en las trazas.
#IA_MODELO=local

# Cuantas herramientas puede ENCADENAR el modelo para una sola pregunta.
#
# Hasta que existio esto el bucle daba una consulta y punto, y con una sola
# consulta la pregunta que mas importa —"por que fallo esto?"— no se puede
# contestar: hace falta el estado, la historia de la senal sospechosa y a
# veces el manual. Tres cubre el diagnostico completo.
#
# Es configurable porque el precio lo pone el hardware, no el codigo: en una
# GPU holgada cada paso son segundos; con el modelo a medias en CPU, tres
# pasos son un par de minutos de espera y quiza convenga bajarlo a 2. El
# backend no ejecuta mas de IA_MAX_PASOS*2 herramientas por turno, y una
# consulta identica repetida no se vuelve a ejecutar.
#IA_MAX_PASOS=3

# Carpeta con la documentacion de planta: manuales, hojas de datos,
# procedimientos. VACIO por defecto: sin esto, la herramienta de consultar
# documentacion dice que este servidor no tiene manuales cargados en vez de
# contestar de memoria sobre un equipo que nadie le ha descrito.
#
# Se leen .txt, .md, .csv, .log y .pdf. Del PDF se extrae el texto sin
# dependencias (zlib de Node), lo que cubre los manuales generados por Word o
# InDesign; un PDF ESCANEADO es una imagen y no se puede leer sin OCR — el
# indice lo detecta y lo dice en vez de indexar basura.
#
# La ruta puede ser absoluta o relativa a la raiz del proyecto. La carga es
# perezosa: no retrasa el arranque, se hace en la primera consulta.
#
# Se llama "Documentacion" y no "Documentos" (Plan 16): "Documentos" pasa a
# ser la carpeta de lo que este backend GENERA -reportes, exportaciones de
# chat, ver IA_REPORTES_DIR e IA_BACKLOG_CHAT_DIR mas abajo-, y esta es la de
# lo que alguien LE DA de fuera para que lo lea. Dos significados distintos
# no pueden compartir nombre de carpeta sin confundir a quien mire el disco.
#IA_DOCS_DIR=Documentacion

# Segundo llama-server, SOLO para embeddings de la documentacion.
#
# VACIO por defecto, y ese defecto es la decision: llama-server sirve un
# modelo a la vez, asi que el que atiende el chat no puede ademas generar
# embeddings, y pedirselos devuelve los de un modelo generativo, que para
# buscar son malos. Sin esto la documentacion se busca con BM25 —lexico, sin
# servidor, sin dependencias—, que en manuales tecnicos acierta porque quien
# pregunta usa el vocabulario del manual.
#
# Si se configura, apunta a un llama-server arrancado con --embedding sobre un
# modelo de embeddings, y los dos metodos se mezclan (60% semantico, 40%
# lexico): el coseno encuentra "como se pone a cero" cuando el manual dice
# "ajuste del offset", y BM25 clava las referencias tipo "PMP63B" o "F270",
# que un embedding disuelve.
#IA_EMBEDDING_BASE=http://127.0.0.1:8081
#IA_EMBEDDING_MODELO=local

# Permite subir, reemplazar y archivar manuales desde el cajon «Manuales»
# (POST/PUT/PATCH /api/rag/documentos, Plan 16 Fase 1).
#
# FALSE por defecto, y el defecto importa: ICONICS_READ_ONLY NO cubre esto
# —protege escrituras contra el PLC, no escrituras en el disco de este
# backend—, asi que sin su propia bandera la carga de manuales quedaria
# habilitada por accidente en cualquier instalacion que ya tenga
# ICONICS_READ_ONLY=false para otra cosa (controlar la bomba, por ejemplo).
#
# Con IA_DOCS_DIR vacio da igual lo que valga esto: sin carpeta de
# documentacion no hay donde subir nada.
#RAG_UPLOAD_ENABLED=false

# IA_MAQUINAS_CON_HISTORIA se retiro: el asistente responde sobre el sistema
# de agua (ac:TDCON/DEMO/SENSORES/) y alli que una senal tenga historia no es
# configurable. A tres de las ocho el historiador les devuelve LA SERIE DE
# OTRA sin dar error, asi que la marca vive como hecho medido en
# shared/eva/senales.js (campo `historizado`) y no en una variable de entorno.

# Carpeta de salida de los reportes PDF que genera la herramienta
# generar_reporte (Plan 14 Fase 5). A diferencia de IA_DOCS_DIR, vacio NO
# desactiva nada: no depende de ningun sistema externo, solo de disco, y la
# carpeta se crea sola en la primera escritura.
#
# Dentro de Documentos/ (Plan 16), junto a BacklogChat/: las dos son PDF que
# este backend genera, no algo que alguien le dio para leer -eso es
# Documentacion/, ver IA_DOCS_DIR mas arriba-.
#
# Relativa a la raiz del proyecto, o absoluta.
#IA_REPORTES_DIR=Documentos/Reportes

# Antiguedad, en dias, a partir de la cual un reporte se purga solo. La purga
# es perezosa -se dispara en la propia escritura del siguiente reporte, sin
# temporizador- mismo criterio que la cache de lote de iconics/client.mjs.
# Solo aplica a IA_REPORTES_DIR: la exportacion de chat (IA_BACKLOG_CHAT_DIR,
# justo debajo) no se purga sola, porque exportar una conversacion es una
# decision explicita de alguien de conservarla.
#IA_REPORTES_MAX_DIAS=30

# Carpeta de salida de los PDF que genera el boton "Exportar PDF" del chat
# (POST /api/chat/exportar). Separada de IA_REPORTES_DIR desde el Plan 16:
# antes las dos compartian carpeta, distinguidas solo por el UUID del
# nombre, y mezclaban dos cosas de origen distinto -trabajo del asistente
# sobre una pregunta de datos, frente a un registro de lo que se hablo- bajo
# un mismo rotulo. GET /api/reportes sigue sirviendo las dos carpetas por
# igual: no hace falta cambiar ningun enlace ya generado.
#
# Relativa a la raiz del proyecto, o absoluta. Igual que IA_REPORTES_DIR,
# vacio NO desactiva nada y la carpeta se crea sola en la primera escritura.
#IA_BACKLOG_CHAT_DIR=Documentos/BacklogChat

# Horario de turnos, para poder preguntar "el turno de la manana del 20 de
# julio". VACIO por defecto, y ese defecto es la decision: sin el horario
# real de esta planta, un turno inventado devolveria datos verdaderos de las
# horas equivocadas, que es indistinguible de la respuesta correcta. Con esto
# vacio, preguntar por un turno responde que no esta configurado y ofrece
# preguntar por una hora concreta.
#
# Formato: nombre=horaInicio-horaFin, separados por comas. Un turno de noche
# puede cruzar la medianoche (22-6).
#IA_TURNOS=manana=6-14,tarde=14-22,noche=22-6

# ── Contexto de /api/context ────────────────────────────────────────
# Un usuario/linea/equipo/turno FIJOS de configuracion, no de la sesion real.
# Ningun consumidor del frontend de esta rama llama hoy a /api/context -el
# login nativo del Plan 20 ya sabe quien pregunta por la cookie de sesion-,
# asi que estas variables quedan sin efecto visible salvo que algo externo
# siga pidiendo esa ruta. Se documentan en vez de borrarse porque la ruta
# sigue viva en backend/routes/systemRoutes.mjs y no es parte de este plan.
#DEFAULT_USUARIO=Operador
#DEFAULT_LINEA=Linea 1
#DEFAULT_EQUIPO=Equipo principal
#DEFAULT_TURNO=Matutino
#DEFAULT_RENDIMIENTO=84%

# ── El frontend NO se configura aqui ──────────────────────────────────
# VITE_API_BASE puede declararse en react-dashboard/.env.local o en el
# entorno del proceso. VITE_BASE_PATH se lee en vite.config.js: pasala en
# el entorno del proceso de build (scripts/dev.ps1 configura ambas).
# Cambiarlas requiere recompilar; este .env del backend no las configura.
#
#   VITE_API_BASE           Base del backend. Vacia = mismo origen, que es
#                           lo correcto en produccion (el puente sirve el
#                           bundle). En desarrollo usa el proxy de Vite.
#   VITE_BASE_PATH          Subruta publica del bundle (por ejemplo /asistente/).
#
# El Plan 20 desinstalo el transporte simulado del frontend, el selector de
# origen y la vista de Prediccion junto con el tablero: VITE_ICONICS_FAKE,
# VITE_ICONICS_CHAOS, VITE_ENABLE_SIMULATOR y VITE_PREDICTION_* ya no las lee
# nada. El transporte falso del BACKEND (ICONICS_FAKE, mas arriba) sigue
# vivo y es el camino recomendado para desarrollar sin servidor.

# ── Certificados ──────────────────────────────────────────────────────
# Desactiva la verificacion de certificados de TODO el proceso, no solo de
# la llamada a ICONICS. En desarrollo hace falta por el certificado
# auto-firmado del servidor de planta; con NODE_ENV=production el arranque
# falla a proposito antes que arrancar inseguro.
#NODE_TLS_REJECT_UNAUTHORIZED=0

# La solucion buena, y la unica valida en el servidor: dar de alta la CA de
# ICONICS en vez de apagar la verificacion. La lee Node directamente, no
# este backend, asi que no aparece en config.mjs.
#NODE_EXTRA_CA_CERTS=/ruta/a/iconics-ca.crt

# ── Dictado por voz (whisper.cpp) ─────────────────────────────────────
#
# Apunta a `whisper-server`, que se arranca aparte igual que llama-server:
#
#   whisper.cppin\Release\whisper-server.exe ^
#     -m whisper.cpp\models\ggml-small.bin -l es -t 4 --port 8082 --host 127.0.0.1
#
# (hay un atajo en scripts/whisper.ps1)
#
# VACIO por defecto: sin esto la ruta /api/voz responde 503 diciendo que falta
# la variable, y el panel no pinta el boton del microfono. Misma regla de la
# casa que IA_BASE: una instalacion que nadie configuro no expone una funcion
# a medias.
#
# whisper-server tiene que escuchar en 127.0.0.1, igual que llama-server: no
# lleva autenticacion de ninguna clase.
#
# El audio llega ya convertido a WAV de 16 kHz desde el NAVEGADOR (Web Audio
# API), asi que NO hace falta arrancarlo con --convert ni instalar ffmpeg.
#IA_WHISPER_BASE=http://127.0.0.1:8082

# Idioma del dictado. Fijo y NO 'auto' a proposito: con deteccion automatica,
# una frase corta con ruido de planta se confunde a menudo con portugues o
# italiano, y la transcripcion sale traducida a un idioma que nadie pidio.
#IA_WHISPER_IDIOMA=es

# Corte de una transcripcion. En CPU, whisper small tarda algo menos que la
# duracion del audio; en GPU sobra de largo.
#IA_WHISPER_TIMEOUT_MS=60000
